# gitbook-plugin-pageload-analytics

GitBook plugin for pageload analytics using Google Analytics.

`book.json`:

```
{
    "plugins": ["pageload-analytics"],
    "pluginsConfig": {
        "pageload-analytics": {
            "token": "UA-XXXXXXXX-X"
        }
    }
}

```
